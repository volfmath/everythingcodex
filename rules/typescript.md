# TypeScript Rules

- prefer explicit return types for exported functions
- avoid `any` unless justified
- keep data shapes narrow
- prefer small modules over giant utility files
- make runtime validation explicit at boundaries
