# Preflight Checklist

Before finalizing:

- changed files are intentional
- no secrets were added
- validation was run or explicitly skipped
- docs were updated if behavior changed
- unrelated files were not modified
