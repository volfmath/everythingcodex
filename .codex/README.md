# Codex Surface

This folder is reserved for Codex-specific workspace assets.

Recommended usage:

- keep shared workflow material under `.codex/everything-codex/`
- keep repo-specific overrides in the target repo root `AGENTS.md`
- do not duplicate the same rule in multiple places unless necessary
