Add this to the target repo root `AGENTS.md` if you want to reference this kit:

```md
## Workflow Surface

Use `.codex/everything-codex/skills/` as the shared skill library.
Use `.codex/everything-codex/rules/` as the shared rule library.
Use `.codex/everything-codex/agents/` for standard delegated roles.

Default operating pattern:
- onboard first for unfamiliar repos
- plan before large edits
- review before final handoff
```
